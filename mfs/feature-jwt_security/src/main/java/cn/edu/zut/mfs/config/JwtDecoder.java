package cn.edu.zut.mfs.config;
public class JwtDecoder {
 /*   @Override
    public Mono<Jwt> decode(String token) throws JwtException {
        return Mono.just(new Jwt(token, Instant.now(),Instant.MAX,null,null));
    }*/
}
